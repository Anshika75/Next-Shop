// Use Google map api and show a map on screen in new react version using functional component

import React from 'react';

const GoogleMap = (props) => {

    return (
        <div className="google-map" ref="map" />
    );
}

export default GoogleMap;